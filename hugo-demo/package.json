{
    "name": "hugomplate",
    "version": "0.0.1",
    "license": "MIT",
    "scripts": {
        "getgomplate": "npm install -g gomplate",
        "build:gomplate": "gomplate",
        "build:hugo": "hugo",
        "build": "npm run build:gomplate && npm run build:hugo",
        "deploy": "npm run getgomplate && npm run build"
    }
}
